apiVersion: v1
kind: Service
metadata:
  name: hugio-app-hugibot-svc
  labels:
    app: hugio-app-hugibot
spec:
  type: NodePort
  selector:
    app: hugio-app-hugibot
  ports:
    - protocol: 'TCP'
      name: http
      port: 4208
      targetPort: 90
